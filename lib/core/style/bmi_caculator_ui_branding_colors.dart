import 'package:flutter/material.dart';

class BmiCalculatorUiBrandingColors {
  /// Base to primary pure color
  ///
  /// Figma token: $branding-color-primary-pure
  static const Color greenVogue = Color(0xFF023047);
  static const Color chatamsBlue = Color(0xFF175574);
  static const Color flushOrange = Color(0xFFFB8500);
  static const Color whitePure = Color(0xFFFFFFFF);
}
